This Project is about dynamic search using React.
A list of JSON data is given and user can search word from that list by typing through search box.

Run the project by instaling npm packages.
npm install --save
